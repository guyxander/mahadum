import Link from "next/link";import {requestPasswordReset} from "@/app/auth/actions";
export default async function ForgotPassword({searchParams}:{searchParams:Promise<{error?:string;message?:string}>}){const {error,message}=await searchParams;return <main className="auth-page"><section className="auth-brand"><Link className="brand" href="/"><span className="brand-mark">M</span> Mahadum</Link><div><span className="overline light">Account recovery</span><h1>Find your way back.</h1><p>We will send a secure recovery link to your confirmed email address.</p></div></section><section className="auth-form"><div><h2>Reset your password</h2><p>Enter the email connected to your account.</p>{error&&<div className="auth-alert" role="alert">{error}</div>}{message&&<div className="auth-notice" role="status">{message}</div>}<form action={requestPasswordReset}><label>Email address<input type="email" name="email" autoComplete="email" required/></label><button className="button">Send recovery link</button></form><p className="auth-switch"><Link href="/login">← Return to login</Link></p></div></section></main>}
