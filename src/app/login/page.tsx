import Link from "next/link";
import { GoogleAuthButton } from "@/components/google-auth-button";

export default async function LoginPage({ searchParams }: { searchParams: Promise<{ message?: string; next?: string }> }) {
  const { message, next: requestedNext } = await searchParams;
  const next = requestedNext?.startsWith("/") && !requestedNext.startsWith("//") ? requestedNext : undefined;
  return <main className="auth-page auth-page-google"><section className="auth-brand"><Link className="brand" href="/"><span className="brand-mark">M</span> Mahadum</Link><div><span className="overline light">One account. Every possibility.</span><h1>Learn. Teach. Grow.</h1><p>Move between your learning space and Creator Hub with one secure account.</p></div><small>Built for ambitious Africans.</small></section><section className="auth-form"><div className="auth-google-card"><span className="auth-welcome-mark">Welcome</span><h2>Continue to Mahadum</h2><p>Use your Google account to sign in or create an account.</p>{message&&<div className="auth-notice" role="status">{message}</div>}<GoogleAuthButton next={next}/><div className="auth-mobile-points" aria-label="Account benefits"><span><b>Learn</b> practical skills</span><span><b>Teach</b> your expertise</span><span><b>Earn</b> from your audience</span></div><div className="auth-security-note"><span aria-hidden="true">✓</span><p>Secure Google authentication. Mahadum never sees or stores your Google password.</p></div><p className="auth-switch">By continuing, you agree to our <Link href="/legal/terms">Terms</Link> and <Link href="/legal/privacy">Privacy Policy</Link>.</p></div></section></main>;
}
