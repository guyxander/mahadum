import Link from "next/link";
import {updatePassword} from "@/app/auth/actions";
export default async function ResetPassword({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="auth-page"><section className="auth-brand"><Link className="brand" href="/"><span className="brand-mark">M</span> Mahadum</Link><div><span className="overline light">Secure your account</span><h1>Choose a new password.</h1><p>Use at least eight characters with uppercase, lowercase, and numbers.</p></div></section><section className="auth-form"><div><h2>New password</h2>{error&&<div className="auth-alert" role="alert">{error}</div>}<form action={updatePassword}><label>Password<input type="password" name="password" minLength={8} autoComplete="new-password" required/></label><label>Confirm password<input type="password" name="confirm" minLength={8} autoComplete="new-password" required/></label><button className="button">Update password</button></form></div></section></main>}
