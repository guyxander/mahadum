import type { MetadataRoute } from "next";
export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://mahadum.vercel.app";return ["","/courses","/login","/signup","/support","/legal/terms","/legal/privacy","/legal/refunds"].map(path=>({url:`${base}${path}`,lastModified:new Date(),changeFrequency:path==="/courses"?"daily":path?"monthly":"weekly",priority:path==="/courses"?0.9:path?0.6:1}))}
