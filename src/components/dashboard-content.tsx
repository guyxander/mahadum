import Link from "next/link";
import type {DashboardData,Row} from "@/lib/dashboard";
import {addLesson,addModule,deleteCourse,deleteLesson,deleteModule,moderateCourse,moderateCreator,saveCourse,submitCourse,updateLesson,updateModule,updateProfile} from "@/app/dashboard/actions";
import {CheckoutButton} from "@/components/checkout-button";

const string=(value:unknown)=>typeof value==="string"?value:"";
const number=(value:unknown)=>typeof value==="number"?value:0;
const object=(value:unknown)=>value&&typeof value==="object"&&!Array.isArray(value)?value as Row:{};
const list=(value:unknown)=>Array.isArray(value)?value as Row[]:[];
const money=(minor:number,currency="NGN")=>new Intl.NumberFormat("en-NG",{style:"currency",currency,maximumFractionDigits:0}).format(minor/100);
const date=(value:unknown)=>value?new Intl.DateTimeFormat("en-NG",{dateStyle:"medium"}).format(new Date(String(value))):"—";

export function DashboardContent({role,section,data}:{role:string;section:string;data:DashboardData}){
  if(role==="creator"&&section==="course-builder")return <CourseBuilder data={data}/>;
  if(role==="creator"&&section==="courses")return <CreatorCourses data={data}/>;
  if(role==="learner"&&section==="my-learning")return <MyLearning data={data}/>;
  if(role==="learner"&&section==="explore")return <Explore data={data}/>;
  if(role==="learner"&&section==="certificates")return <Certificates data={data}/>;
  if(section==="profile")return <Profile data={data}/>;
  if(role==="admin")return <AdminSection section={section} data={data}/>;
  return <Overview role={role} data={data}/>;
}

function CourseBuilder({data}:{data:DashboardData}){return <div className="dashboard-page"><PageHead title="Course builder" subtitle="Create real courses, modules, and lessons. Drafts remain private until submitted."/><section className="panel"><h3>New course</h3><CourseForm categories={data.categories}/></section>{data.courses.map(course=><section className="panel course-editor" key={string(course.id)}><div className="panel-head"><div><span className={`status ${string(course.status)}`}>{string(course.status)}</span><h3>{string(course.title)}</h3></div></div><details><summary>Edit course details</summary><CourseForm categories={data.categories} course={course}/></details><form action={addModule} className="inline-form"><input type="hidden" name="course_id" value={string(course.id)}/><input name="title" required placeholder="New module title"/><button className="outline-button">Add module</button></form>{list(course.course_modules).sort((a,b)=>number(a.position)-number(b.position)).map(module=><div className="module-editor" key={string(module.id)}><form action={updateModule} className="inline-form"><input type="hidden" name="id" value={string(module.id)}/><input name="title" defaultValue={string(module.title)} required/><button className="outline-button">Rename</button></form>{list(module.lessons).sort((a,b)=>number(a.position)-number(b.position)).map(lesson=><details className="lesson-row" key={string(lesson.id)}><summary>{number(lesson.position)+1}. {string(lesson.title)} · {Math.ceil(number(lesson.duration_seconds)/60)} min</summary><form action={updateLesson} className="crud-form compact"><input type="hidden" name="id" value={string(lesson.id)}/><input name="title" defaultValue={string(lesson.title)} required/><input name="video_url" type="url" defaultValue={string(lesson.video_url)}/><input name="duration_minutes" type="number" min="0" defaultValue={Math.ceil(number(lesson.duration_seconds)/60)}/><textarea name="description" defaultValue={string(lesson.description)}/><label className="check"><input name="is_free_preview" type="checkbox" defaultChecked={Boolean(lesson.is_free_preview)}/> Free preview</label><button className="outline-button">Update lesson</button></form><form action={deleteLesson}><input type="hidden" name="id" value={string(lesson.id)}/><button className="danger-button">Delete lesson</button></form></details>)}<form action={addLesson} className="crud-form compact"><input type="hidden" name="module_id" value={string(module.id)}/><input name="title" required placeholder="Lesson title"/><input name="video_url" type="url" placeholder="Secure video URL"/><input name="duration_minutes" type="number" min="0" placeholder="Minutes"/><textarea name="description" placeholder="Lesson description"/><label className="check"><input name="is_free_preview" type="checkbox"/> Free preview</label><button className="outline-button">Add lesson</button></form><form action={deleteModule}><input type="hidden" name="id" value={string(module.id)}/><button className="danger-button">Delete module</button></form></div>)}</section>)}</div>}

function CourseForm({categories,course}:{categories:Row[];course?:Row}){return <form action={saveCourse} className="crud-form"><input type="hidden" name="id" value={string(course?.id)}/><label>Course title<input name="title" defaultValue={string(course?.title)} required/></label><label>Short description<input name="short_description" defaultValue={string(course?.short_description)} required maxLength={180}/></label><label>Full description<textarea name="description" defaultValue={string(course?.description)} required/></label><div className="form-row"><label>Category<select name="category_id" defaultValue={string(course?.category_id)} required><option value="">Select category</option>{categories.map(x=><option value={string(x.id)} key={string(x.id)}>{string(x.name)}</option>)}</select></label><label>Price (NGN)<input name="price" type="number" min="0" step="100" defaultValue={course?number(course.price_minor)/100:undefined} required/></label></div><button className="button">{course?"Update course":"Save draft"}</button></form>}

function CreatorCourses({data}:{data:DashboardData}){return <div className="dashboard-page"><PageHead title="Courses" subtitle="Manage drafts and submit completed courses for review." action="Create course" actionHref="/dashboard/creator/course-builder"/><section className="panel table-panel">{data.courses.length===0?<Empty text="No courses yet. Create your first draft."/>:data.courses.map(course=><div className="data-row" key={string(course.id)}><span className="user-avatar">{string(course.title).slice(0,2).toUpperCase()}</span><div><b>{string(course.title)}</b><small>{money(number(course.price_minor),string(course.currency)||"NGN")} · {date(course.updated_at)}</small></div><span>{string(object(course.categories).name)||"Uncategorised"}</span><span className={`status ${string(course.status)}`}>{string(course.status)}</span><div className="row-actions">{course.status==="draft"&&<><form action={submitCourse}><input type="hidden" name="id" value={string(course.id)}/><button>Submit</button></form><form action={deleteCourse}><input type="hidden" name="id" value={string(course.id)}/><button>Delete</button></form></>}</div></div>)}</section></div>}

function MyLearning({data}:{data:DashboardData}){return <div className="dashboard-page"><PageHead title="My learning" subtitle="Continue courses purchased through your account." action="Explore" actionHref="/dashboard/learner/explore"/><div className="learning-grid">{data.enrollments.length===0?<Empty text="You are not enrolled in a course yet."/>:data.enrollments.map(enrollment=>{const course=object(enrollment.courses);const lessons=list(course.course_modules).flatMap(m=>list(m.lessons));const done=new Set(data.progress.filter(p=>p.enrollment_id===enrollment.id&&p.completed_at).map(p=>p.lesson_id));const percent=lessons.length?Math.round(done.size/lessons.length*100):0;const next=lessons.find(x=>!done.has(x.id))||lessons[0];return <article className="learning-card" key={string(enrollment.id)}><div className="learning-cover"><span>{percent}%</span></div><div><small>{enrollment.completed_at?"COMPLETED":"IN PROGRESS"}</small><h3>{string(course.title)}</h3><p>{string(course.short_description)}</p><div className="progress"><span style={{width:`${percent}%`}}/></div>{next&&<Link href={`/learn/${string(course.id)}/${string(next.id)}`}>Continue learning →</Link>}</div></article>})}</div></div>}

function Explore({data}:{data:DashboardData}){return <div className="dashboard-page"><PageHead title="Explore courses" subtitle="Published courses from verified Mahadum creators."/><div className="learning-grid">{data.courses.length===0?<Empty text="No courses have been published yet."/>:data.courses.map(course=><article className="learning-card" key={string(course.id)}><div className="learning-cover"><span>{string(object(course.categories).name)||"Course"}</span></div><div><small>{money(number(course.price_minor),string(course.currency)||"NGN")}</small><h3>{string(course.title)}</h3><p>{string(course.short_description)}</p><CheckoutButton courseId={string(course.id)}/></div></article>)}</div></div>}

function Certificates({data}:{data:DashboardData}){return <div className="dashboard-page"><PageHead title="Certificates" subtitle="Verifiable proof of completed learning."/>{data.certificates.length===0?<Empty text="Certificates appear automatically after every lesson is completed."/>:data.certificates.map(item=>{const enrollment=object(item.enrollments);return <div className="certificate-sheet" key={string(item.id)}><span className="brand-mark">M</span><small>CERTIFICATE OF COMPLETION</small><h2>{string(object(enrollment.courses).title)}</h2><p>This certifies that</p><h3>{data.name}</h3><p>successfully completed this course on {date(enrollment.completed_at)}.</p><div><b>Mahadum</b><b>{string(item.verification_code).toUpperCase()}</b></div></div>})}</div>}

function Profile({data}:{data:DashboardData}){const profile=data.profile||{};return <div className="dashboard-page"><PageHead title="Profile" subtitle="Keep your public and account information current."/><section className="panel"><form action={updateProfile} className="crud-form"><label>Full name<input name="full_name" defaultValue={string(profile.full_name)} required/></label><label>Username<input name="username" defaultValue={string(profile.username)}/></label><label>Bio<textarea name="bio" defaultValue={string(profile.bio)}/></label><label>Country code<input name="country_code" defaultValue={string(profile.country_code)} maxLength={2}/></label><button className="button">Save profile</button></form></section></div>}

function AdminSection({section,data}:{section:string;data:DashboardData}){let source:Row[]=data.users;if(section==="courses")source=data.courses;else if(section==="creators")source=data.creators;else if(section==="affiliates")source=data.affiliates;else if(section==="finance")source=[...data.payments,...data.refunds,...data.payouts];else if(section==="audit-log")source=data.audit;else if(section==="learners")source=data.users.filter(x=>list(x.user_roles).some(r=>r.role==="learner"));
  if(section==="overview"||section==="analytics"||section==="settings")return <Overview role="admin" data={data}/>;
  return <div className="dashboard-page"><PageHead title={section.replaceAll("-"," ")} subtitle={`Live ${section.replaceAll("-"," ")} records from Supabase.`}/><section className="panel table-panel">{source.length===0?<Empty text={`No ${section.replaceAll("-"," ")} records yet.`}/>:source.map((row,index)=>{const title=string(row.title)||string(row.full_name)||string(object(row.profiles).full_name)||string(row.action)||string(row.tx_ref)||`Record ${index+1}`;const status=string(row.status)||string(row.verification_status)||"active";return <div className="data-row" key={string(row.id)||string(row.user_id)||`${section}-${index}`}><span className="user-avatar">{title.slice(0,2).toUpperCase()}</span><div><b>{title}</b><small>{string(row.email)||string(row.entity_type)||date(row.created_at||row.requested_at)}</small></div><span>{row.amount_minor!==undefined?money(number(row.amount_minor),string(row.currency)||"NGN"):date(row.created_at)}</span><span className={`status ${status}`}>{status}</span><div className="row-actions">{section==="courses"&&status==="in_review"&&<><ModerateCourse id={string(row.id)} status="published" label="Publish"/><ModerateCourse id={string(row.id)} status="rejected" label="Reject"/></>}{section==="creators"&&status==="pending"&&<><ModerateCreator id={string(row.user_id)} status="verified" label="Verify"/><ModerateCreator id={string(row.user_id)} status="rejected" label="Reject"/></>}</div></div>})}</section></div>}

function ModerateCourse({id,status,label}:{id:string;status:string;label:string}){return <form action={moderateCourse}><input type="hidden" name="id" value={id}/><input type="hidden" name="status" value={status}/><button>{label}</button></form>}
function ModerateCreator({id,status,label}:{id:string;status:string;label:string}){return <form action={moderateCreator}><input type="hidden" name="id" value={id}/><input type="hidden" name="status" value={status}/><button>{label}</button></form>}
function Overview({role,data}:{role:string;data:DashboardData}){const revenue=data.ledger.filter(x=>number(x.amount_minor)>0).reduce((sum,x)=>sum+number(x.amount_minor),0);const metrics=role==="admin"?[{label:"Users",value:data.users.length},{label:"Courses",value:data.courses.length},{label:"Verified payments",value:data.payments.filter(x=>x.status==="successful").length},{label:"Ledger volume",value:money(data.ledger.reduce((s,x)=>s+Math.max(0,number(x.amount_minor)),0))}]:[{label:"Courses",value:data.courses.length},{label:"Settled earnings",value:money(revenue)},{label:"Payout requests",value:data.payouts.length},{label:"Affiliate status",value:string(data.affiliates[0]?.status)||"Not enrolled"}];return <div className="dashboard-page"><PageHead title={`Welcome, ${data.name}`} subtitle="Live operational data—no demonstration records."/><div className="metric-grid">{metrics.map(x=><article className="metric-card" key={x.label}><span>{x.label}</span><strong>{x.value}</strong></article>)}</div><section className="panel"><h3>Recent activity</h3><p>{role==="admin"?`${data.audit.length} recent audited events available.`:data.courses.length?"Your course and earnings records are current.":"Create your first course to begin."}</p></section></div>}
function PageHead({title,subtitle,action,actionHref}:{title:string;subtitle:string;action?:string;actionHref?:string}){return <div className="page-head"><div><h1>{title}</h1><p>{subtitle}</p></div>{action&&actionHref&&<Link className="button button-small" href={actionHref}>{action}</Link>}</div>}
function Empty({text}:{text:string}){return <div className="empty-state"><b>Nothing here yet</b><p>{text}</p></div>}
